#include"u_ctypes.h"
#include"u_heap.h"

char ntoc(uint8_t val);

u64 pof(u64 base, u64 exponent);

char* ulltoa(uint64_t val);

size_t strlen(const char* str, size_t maxSize);

char toupper(char ch);